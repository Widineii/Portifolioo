/** URL pública do site (GitHub Pages). Usado em meta tags e imagens absolutas. */
export const SITE_ORIGIN = "https://widineii.github.io";
export const SITE_BASE_PATH = "/Portifolioo/";
export const SITE_URL = `${SITE_ORIGIN}${SITE_BASE_PATH.replace(/\/$/, "")}/`;

/** Prints dos projetos hospedados no outro repositório (site estático). */
export const STATIC_PORTFOLIO_PROJECT_ASSETS = `${SITE_ORIGIN}/Portifolio/assets/projects`;
